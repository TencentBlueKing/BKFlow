# V4.0.0 API 插件节点 · 历史失效版本回看态

[[ 首页 > 流程 > 模板编辑 > 节点配置 ]]

::: card
#### 场景
历史模板绑定的 plugin_version 已不在当前目录 versions 中。前端展示历史快照，但禁止继续基于该版本保存或新建任务。

((只读回看)){error}
:::

::: row
::: card
#### 画布节点
云作业执行脚本

((历史快照)){primary}
((plugin_version 1.2.0)){error}

> 节点仍可打开查看，但需要明确这是历史 schema 快照。

<!-- ❶ 历史失效版本仍可回看，不能让用户误以为该版本仍可继续新用。 -->
:::

::: card
#### 版本不可用
当前版本 1.2.0 已不在标准运维开放插件目录返回的 versions 中。

((不可保存)){error}
((不可新建任务)){error}

<!-- ❷ 版本不可用提示需要明确影响范围：可看历史参数，不可保存为可执行配置，不可基于该版本创建或启动任务。 -->

#### 历史参数快照
业务 ID
[200145___]

执行账号
[root___]

脚本内容
[echo legacy deploy___]

超时时间
[300___]

<!-- ❸ 参数控件进入只读态。展示内容来自插件引用快照和 schema 快照，不实时请求已失效版本 schema。 -->

::: row {right}
[保存节点配置]{secondary} [切换到可用版本]*
:::

<!-- ❹ 保存按钮禁用或提交前拦截；主路径是切换到可用版本后重新拉 schema、重新确认参数。 -->
:::

::: card
#### 可切换版本
| 版本 | 状态 | 建议 |
|------|------|------|
| 1.5.0 | 可用 / 最新 | 推荐新模板使用 |
| 1.4.2 | 可用 / 默认 | 与历史参数更接近 |

#### 服务端强校验
保存模板、创建任务、启动任务都会校验：

- source grant
- 空间 per-plugin enabled
- plugin_id 存在
- plugin_version 仍在 catalog.versions

<!-- ❺ 前端禁用只是体验优化，不能替代服务端强校验；错误文案优先使用后端返回的结构化原因。 -->
:::
:::
