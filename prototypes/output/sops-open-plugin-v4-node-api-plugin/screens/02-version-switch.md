# V4.0.0 API 插件节点 · 切换业务版本态

[[ 首页 > 流程 > 模板编辑 > 节点配置 ]]

::: card
#### 场景
用户把节点从已保存版本 1.4.2 切换到 latest_version 1.5.0，前端需要重新拉取 schema 并提示参数变化。

((有未保存变更)){primary}
:::

::: row
::: card
#### 当前节点
云作业执行脚本

((已保存 1.4.2)){primary}
((目标 1.5.0)){success}

> 版本切换后，画布节点或配置抽屉顶部需要提示存在未保存变更。

<!-- ❶ 切换 plugin_version 不自动保存，也不自动升级历史模板；只有用户点击保存后才写入节点。 -->
:::

::: card
#### 版本选择
当前保存版本
[1.4.2___]

目标版本
[1.5.0 最新版本___]

<!-- ❷ 选择目标版本后，前端带 plugin_version=1.5.0 重新请求 schema。schema 拉取失败时保留旧配置并展示错误提示。 -->

#### 差异摘要
| 类型 | 数量 | 说明 |
|------|------|------|
| 保留字段 | 3 | 业务 ID / 执行账号 / 脚本内容 |
| 新增必填 | 1 | 结果归档路径 |
| 默认值变化 | 1 | 超时时间 600 -> 900 |

结果归档路径
[请选择归档目录___]

((必填未完成)){error}

<!-- ❸ 同 key 字段尽量保留旧值；新增必填字段为空时，保存前校验失败，字段展示错误态。 -->
:::

::: card
#### 可选版本
| 版本 | 状态 | 说明 |
|------|------|------|
| 1.5.0 | 可用 / 最新 | 新增执行结果归档字段 |
| 1.4.2 | 可用 / 当前 | 当前已保存版本 |
| 1.2.0 | 不可用 | 不在当前 versions 中 |

<!-- ❹ 版本列表来自目录字段 versions/default_version/latest_version。前端只作为展示和候选项，最终仍以服务端校验为准。 -->

#### 保存确认
版本切换会影响参数 schema，请确认新增字段与默认值变化。

[继续编辑] [确认并保存]*

<!-- ❺ 存在 schema 差异时，保存前给二次确认；确认后写入 uniform_api_plugin_version 和新的隐藏调度字段。 -->
:::
:::
